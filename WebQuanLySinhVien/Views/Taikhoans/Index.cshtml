@model IEnumerable<WebQuanLySinhVien.Models.Taikhoan>

@{
    ViewData["Title"] = "Index";
}

<h1>Danh sách tài khoản</h1>

<p>
    <button class="btn btn-primary create-btn">Tạo mới</button>
</p>
<div class="row">
    <div class="col-md-6">
        <form class="d-flex align-items-center" asp-action="Index" asp-controller="Taikhoans">
            <div class="row">
                <div class="col-md-8">
                    <input name="searchString" class="form-control me-1" type="search" placeholder="Tìm kiếm" aria-label="Tìm kiếm" value="@ViewData["searchString"]" />
                </div>
                <div class="col-md-4">
                    <button class="btn btn-primary" type="submit">Tìm kiếm</button>
                </div>
            </div>
        </form>
    </div>
    <div class="col-md-6">
        <a asp-action="Index" asp-controller="Taikhoans">Trở về danh sách đầy đủ</a>
    </div>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenDangNhap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MatKhau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VaiTro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @* @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenDangNhap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MatKhau)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VaiTro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTk">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTk">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTk">Delete</a>
            </td>
        </tr>
} *@
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdTk)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TenDangNhap)
                </td>
                <td>
                    <span class="password-hidden" id="@item.IdTk">@item.MatKhau</span>
                    @* <span class="password-plain d-none">@item.MatKhau</span> *@
                   @*  <button type="button" onclick="hienMatKhau(@item.IdTk, @item.MatKhau)">Hiện mật khẩu</button> *@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VaiTro)
                </td>
                <td>
                    <button class="btn btn-primary edit-btn" data-id="@item.IdTk">Sửa</button>
                    <button class="btn btn-primary"><a asp-action="Details" asp-route-id="@item.IdTk">Chi tiết</a></button>
                    <button class="btn btn-danger delete-btn" data-id="@item.IdTk">Xóa</button>
                </td>
            </tr>
        }

    </tbody>
</table>

<div class="fixed-table-pagination">
    <div class="pull-left pagination-detail">
        <span class="pagination-info">
            Hiển thị @((ViewBag.CurrentPage - 1) * ViewBag.PageSize + 1) đến @Math.Min(ViewBag.CurrentPage * ViewBag.PageSize, ViewBag.TotalItems)
        </span>
        <span class="page-list">
            <span class="btn-group dropup">
                <button type="button" class="btn btn-outline dropdown-toggle" data-toggle="dropdown">
                    <span class="page-size">@ViewBag.PageSize</span>
                    <span class="caret"></span>
                </button>
                <ul class="dropdown-menu" role="menu">
                    @{
                        var pageSizes = new[] { 5, 10, 25, 50 };
                        foreach (var size in pageSizes)
                        {
                            <li class="@(size == ViewBag.PageSize ? "active" : "")">
                                <a href="@Url.Action("Index", new {
                                    page = 1,
                                    pageSize = size,
                                    searchString = ViewData["searchString"]
                                })">@size</a>
                            </li>
                        }
                    }
                </ul>
            </span> dòng/trang
        </span>
    </div>
    <div class="pull-right pagination">
        <ul class="pagination">
            @{
                int currentPage = ViewBag.CurrentPage;
                int totalPages = ViewBag.TotalPages;
                string searchString = ViewData["searchString"]?.ToString();

                // Nút đầu trang
                <li class="page-first @(currentPage == 1 ? "disabled" : "")">
                    <a href="@Url.Action("Index", new {
                        page = 1,
                        pageSize = ViewBag.PageSize,
                        searchString = searchString
                    })">«</a>
                </li>

                // Nút trang trước
                <li class="page-pre @(currentPage == 1 ? "disabled" : "")">
                    <a href="@Url.Action("Index", new {
                        page = currentPage - 1,
                        pageSize = ViewBag.PageSize,
                        searchString = searchString
                    })">‹</a>
                </li>

                // Các nút trang
                int startPage = Math.Max(1, currentPage - 2);
                int endPage = Math.Min(totalPages, currentPage + 2);

                for (int i = startPage; i <= endPage; i++)
                {
                    <li class="page-number @(i == currentPage ? "active" : "")">
                        <a href="@Url.Action("Index", new {
                            page = i,
                            pageSize = ViewBag.PageSize,
                            searchString = searchString
                        })">@i</a>
                    </li>
                }

                // Nút trang sau
                <li class="page-next @(currentPage == totalPages ? "disabled" : "")">
                    <a href="@Url.Action("Index", new {
                        page = currentPage + 1,
                        pageSize = ViewBag.PageSize,
                        searchString = searchString
                    })">›</a>
                </li>

                // Nút cuối trang
                <li class="page-last @(currentPage == totalPages ? "disabled" : "")">
                    <a href="@Url.Action("Index", new {
                        page = totalPages,
                        pageSize = ViewBag.PageSize,
                        searchString = searchString
                    })">»</a>
                </li>
            }
        </ul>
    </div>
</div>

<script>
    $(document).ready(function() {
        // Xử lý sửa
        $('.edit-btn').click(function() {
            var id = $(this).data('id');
            $.get(`/Taikhoans/Edit/${id}`, function(data) {
                $('#modalBody').html(data);
                $('#actionModal .modal-title').text('Sửa thông tin');
                $('#actionModal').modal('show');
            });
        });

        // Xử lý nút Create
        $('.create-btn').click(function() {
            $.get('/Taikhoans/Create', function(data) {
                $('#modalBody').html(data);
                $('#actionModal .modal-title').text('Thêm mới');
                $('#actionModal').modal('show');
            });
        });

        // Xử lý xóa
        $('.delete-btn').click(function() {
            var id = $(this).data('id');
            $.get(`/Taikhoans/Delete/${id}`, function(data) {
                $('#modalBody').html(data);
                $('#actionModal .modal-title').text('Xác nhận xóa');
                $('#actionModal').modal('show');
            });
        });
    });
</script>