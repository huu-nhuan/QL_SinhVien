@{
    Layout = null;
}
@model Taikhoan


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Registration | CoderGirl</title>
    <link rel="stylesheet" href="../../template/registration.css">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/WebQuanLySinhVien.styles.css" asp-append-version="true" />
    <script src="../../template/custom/sweetalert.min.js"></script>
    <script>
        function validatePasswords() {
            var password = document.getElementById("MatKhau").value;
            var confirmPassword = document.getElementsByName("confirmPassword")[0].value;
            if (password !== confirmPassword) {
                swal({
                  title: "Lỗi!",
                  text: "Mật khẩu không giống nhau",
                  icon: "error",
            });
                return false;
            }
            return true;
        }
    </script>
    <style>
        .container {
            max-width: 800px;
            width: 100%;
            padding: 30px;
            margin: auto;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            box-sizing: border-box;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="registration form">
            <header>Đăng ký</header>
            @using (Html.BeginForm("Registration", "Access", FormMethod.Post, new { onsubmit = "return validatePasswords()" } ))
            {
                <input type="text" placeholder="Nhập Email" name="email" required>
                @Html.TextBoxFor(x => x.TenDangNhap, new { placeholder = "Tên đăng nhập", required = "required", autofocus = "autofocus" })
                @* <input type="password" placeholder="Create a password" name="password" required> *@
                @* @Html.TextBoxFor(x => x.MatKhau, new { type="pasword", placeholder = "Password", required = "required", @id = "MatKhau" }) *@
                @Html.TextBoxFor(x => x.MatKhau, new { type = "password", placeholder = "Mật khẩu" })
                <input type="password" placeholder="Xác nhận lại mật khẩu" name="confirmPassword" required>
                <input type="submit" class="button" value="Xác nhận">
            }
            <div class="signup">
                <span class="signup">
                    Đã có tài khoản?
                    <a href="/Access/Login">Đăng nhập</a>
                </span>
            </div>
        </div>
    </div>
    @if (ViewData["thongbao"] != null)
    {
        <script>
              swal({
                  title: "Lỗi!",
                  text: "@Html.Raw(ViewData["Thongbao"])",
                  icon: "error",
            });
        </script>
    }
</body>
</html>
